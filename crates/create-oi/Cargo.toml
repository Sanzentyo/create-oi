[package]
name = "create-oi"
version = "0.3.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Sans-IO iRobot Create/Roomba Open Interface protocol — types, encoding, decoding, and TypeState robot API"
keywords = ["irobot", "create", "roomba", "robot", "protocol"]
categories = ["hardware-support", "embedded", "no-std"]

[dependencies]
thiserror.workspace = true

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
